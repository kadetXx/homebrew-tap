# kadetXx Homebrew tap

Homebrew cask for [KeyMIDI](https://github.com/kadetXx/keymidi).

```bash
brew install --cask --no-quarantine kadetXx/tap/keymidi
```

`--no-quarantine` is required until releases are signed/notarized with Apple.
